<% 
	var isPopup=false;
	if(typeof filterObjects.popup!='undefined') isPopup=filterObjects.popup=='true';
%>
<% if(!isPopup){ %>
<%layout('../../_common/layout')%>
<% }else{%>
<%layout('../../_common/plain-layout')%>
<% } %>
<% 
	mode='addnew';
%>
<%- partial('./inventory-fiches-form',{}) %>


